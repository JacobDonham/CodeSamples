using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//[System.Serializable]
public class SavePlayerInfo
{/*
    public float[] checkpointPos;
    public bool hasIceGun;
    public bool hasShotgun;

    public SavePlayerInfo (Checkpoint checkpoint)
    {
        checkpointPos = new float[3];
        checkpointPos[0] = checkpoint.transform.position.x;
        checkpointPos[1] = checkpoint.transform.position.y;
        checkpointPos[2] = checkpoint.transform.position.z;

    }

    public void SavePlayer()
    {
        //SaveSystem.SavePlayer(this);
    }
    
    public void LoadPlayer()
    {
        PlayerData data = SaveSystem.LoadPlayer();

        hasIceGun = data.iceGunActive;
        hasShotgun = data.shotGunActive;

        checkpointPos = new float[3];
        checkpointPos[0] = data.checkpointPos[0];
        checkpointPos[1] = data.checkpointPos[1];
        checkpointPos[2] = data.checkpointPos[2];

        
        checkpointPos.x = data.checkpointPos[0];
        checkpointPos.y = data.checkpointPos[1];
        checkpointPos.z = data.checkpointPos[2];
        

    }*/
}
